# 🔥 Что нужно от вас для Firebase Auth

## 📋 Обязательные шаги:

### 1. Firebase проект

1. **Создайте проект в Firebase Console:**
   - Откройте: https://console.firebase.google.com/
   - Нажмите "Добавить проект" или выберите существующий
   - Следуйте инструкциям

2. **Добавьте Android приложение:**
   - В Firebase Console выберите ваш проект
   - Нажмите на иконку Android
   - Package name: `com.example.mst_projectfoto` (или ваш)
   - Скачайте `google-services.json`
   - **Поместите файл в:** `android/app/google-services.json`

3. **Добавьте iOS приложение:**
   - В Firebase Console нажмите на иконку iOS
   - Bundle ID: ваш Bundle ID из Xcode
   - Скачайте `GoogleService-Info.plist`
   - **Поместите файл в:** `ios/Runner/GoogleService-Info.plist`

### 2. Включите Email/Password аутентификацию

1. **В Firebase Console:**
   - Перейдите в: **Authentication** → **Sign-in method**
   - Нажмите на **Email/Password**
   - Включите переключатель **"Enable"**
   - Нажмите **"Save"**

### 3. Обновите build.gradle файлы

**android/build.gradle.kts:**
```kotlin
buildscript {
    dependencies {
        classpath("com.google.gms:google-services:4.4.0")
    }
}
```

**android/app/build.gradle.kts:**
```kotlin
plugins {
    id("com.android.application")
    id("kotlin-android")
    id("dev.flutter.flutter-gradle-plugin")
    id("com.google.gms.google-services")  // Добавьте эту строку
}
```

## ✅ После этого я создам:

1. ✅ Сервис аутентификации (Firebase Auth)
2. ✅ Экраны регистрации и входа
3. ✅ Навигацию между экранами
4. ✅ Интеграцию аналитики для регистрации
5. ✅ Сохранение состояния авторизации

## ⏳ Когда будете готовы:

Скажите "готово" или "файлы добавлены", и я продолжу создание кода!

---

**Важно:** Без этих файлов Firebase Auth не будет работать. Но я могу создать весь код заранее, и он заработает после добавления файлов.



