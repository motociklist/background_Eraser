# 🔥 Настройка Firebase Firestore для хранения API ключей

## ✅ Что уже сделано:

1. ✅ Пакет `cloud_firestore` добавлен в проект
2. ✅ Создан сервис `FirestoreService` для работы с Firestore
3. ✅ Интегрировано сохранение/загрузка API ключа в Firestore
4. ✅ Контроллер обновлен для использования Firestore

## 🚨 ВАЖНО: Включите Firestore в Firebase Console!

**Без этого сохранение API ключей в облаке НЕ будет работать!**

### Пошаговая инструкция:

1. **Откройте Firebase Console:**
   - Перейдите: https://console.firebase.google.com/
   - Выберите проект: **background-editor-248d6**

2. **Создайте базу данных Firestore:**
   - В левом меню нажмите **"Firestore Database"** (База данных Firestore)
   - Если база данных еще не создана, нажмите **"Create database"** (Создать базу данных)

3. **Выберите режим безопасности:**
   - Выберите **"Start in test mode"** (Начать в тестовом режиме) для разработки
   - Или **"Start in production mode"** (Начать в производственном режиме) для продакшена
   - Нажмите **"Next"** (Далее)

4. **Выберите регион:**
   - Выберите ближайший регион (например: `us-central` или `europe-west`)
   - Нажмите **"Enable"** (Включить)

5. **Настройте правила безопасности (для продакшена):**

   Если выбрали production mode, настройте правила:

   ```javascript
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       // Пользователи могут читать и писать только свои данные
       match /users/{userId} {
         allow read, write: if request.auth != null && request.auth.uid == userId;
       }
     }
   }
   ```

   - Перейдите на вкладку **"Rules"** (Правила)
   - Вставьте правила выше
   - Нажмите **"Publish"** (Опубликовать)

## 📊 Структура данных в Firestore:

### Коллекция: `users`
### Документ: `{userId}` (ID пользователя из Firebase Auth)

```json
{
  "apiKey": "ваш_api_ключ",
  "apiProvider": "removebg",
  "blurRadius": 10.0,
  "updatedAt": "2024-01-01T12:00:00Z"
}
```

## 🎯 Как это работает:

1. **При авторизации пользователя:**
   - Приложение автоматически загружает API ключ из Firestore
   - Если ключа нет в Firestore, загружается из локального хранилища (Hive)

2. **При сохранении API ключа:**
   - Если пользователь авторизован → сохраняется в Firestore
   - Также сохраняется в локальное хранилище (fallback)

3. **При изменении провайдера:**
   - Сохраняется в Firestore (если авторизован)
   - Сохраняется в локальное хранилище

4. **При изменении радиуса размытия:**
   - Сохраняется в Firestore (если авторизован)
   - Сохраняется в локальное хранилище

## ✅ Преимущества Firestore:

- 🔄 **Синхронизация между устройствами** - API ключ доступен на всех устройствах пользователя
- ☁️ **Облачное хранилище** - данные не теряются при переустановке приложения
- 🔒 **Безопасность** - данные привязаны к пользователю и защищены правилами безопасности
- ⚡ **Быстрый доступ** - данные загружаются автоматически при входе

## 🆘 Если что-то не работает:

### Ошибка "Missing or insufficient permissions":
- **Причина:** Неправильные правила безопасности в Firestore
- **Решение:** Проверьте правила безопасности (см. шаг 5 выше)

### Ошибка "Firestore not initialized":
- **Причина:** Firestore не включен в Firebase Console
- **Решение:** Выполните шаги 2-4 выше для создания базы данных

### API ключ не сохраняется:
- **Причина:** Пользователь не авторизован
- **Решение:** Убедитесь, что пользователь вошел в аккаунт

### Данные не загружаются:
- **Причина:** Проблемы с интернет-соединением или правилами безопасности
- **Решение:** Проверьте подключение к интернету и правила Firestore

---

**Готово!** После настройки Firestore API ключи будут автоматически сохраняться в облаке! 🎉

