# 📊 План интеграции аналитики и рекламы

## 🎯 Цели аналитики

### Основные метрики для отслеживания:
1. **Пользовательские события** - действия пользователей в приложении
2. **Производительность** - время обработки изображений, ошибки API
3. **Монетизация** - показы рекламы, клики, конверсии
4. **Retention** - возвраты пользователей, частота использования

---

## 📈 События аналитики

### 1. События жизненного цикла приложения
- `app_launched` - Запуск приложения
- `app_foreground` - Приложение на переднем плане
- `app_background` - Приложение на заднем плане

### 2. События выбора изображения
- `image_picked` - Изображение выбрано
  - Параметры: `source` (gallery/camera), `image_size` (bytes)
- `image_pick_cancelled` - Отмена выбора изображения

### 3. События обработки изображений
- `background_removal_started` - Начало удаления фона
  - Параметры: `provider` (removebg/photoroom/clipdrop), `image_size` (bytes)
- `background_removal_completed` - Успешное удаление фона
  - Параметры: `provider`, `duration_ms`, `result_size` (bytes)
- `background_removal_failed` - Ошибка удаления фона
  - Параметры: `provider`, `error_code`, `error_message`
- `background_blur_started` - Начало размытия фона
  - Параметры: `blur_radius`, `provider`, `image_size` (bytes)
- `background_blur_completed` - Успешное размытие фона
  - Параметры: `blur_radius`, `provider`, `duration_ms`, `result_size` (bytes)
- `background_blur_failed` - Ошибка размытия фона
  - Параметры: `blur_radius`, `provider`, `error_code`, `error_message`

### 4. События настроек
- `api_key_entered` - Введен API ключ (без значения ключа!)
- `provider_changed` - Изменен провайдер API
  - Параметры: `provider` (removebg/photoroom/clipdrop)
- `blur_radius_changed` - Изменен радиус размытия
  - Параметры: `blur_radius` (1-50)

### 5. События сохранения
- `image_saved` - Изображение сохранено
  - Параметры: `file_size` (bytes), `platform` (android/ios/web)
- `image_save_failed` - Ошибка сохранения
  - Параметры: `error_message`

### 6. События рекламы
- `ad_loaded` - Реклама загружена
  - Параметры: `ad_type` (banner/interstitial/rewarded), `ad_unit_id`
- `ad_shown` - Реклама показана
  - Параметры: `ad_type`, `ad_unit_id`
- `ad_clicked` - Клик по рекламе
  - Параметры: `ad_type`, `ad_unit_id`
- `ad_failed` - Ошибка загрузки рекламы
  - Параметры: `ad_type`, `error_code`, `error_message`
- `ad_rewarded` - Награда получена (для rewarded ads)
  - Параметры: `ad_type`, `reward_type`, `reward_amount`

### 7. События ошибок
- `api_error` - Ошибка API
  - Параметры: `provider`, `status_code`, `error_message`
- `processing_error` - Ошибка обработки изображения
  - Параметры: `operation` (remove/blur), `error_message`

---

## 💰 План рекламной монетизации

### Типы рекламы:

1. **Banner реклама (Banner Ads)**
   - Размещение: Внизу экрана, всегда видима
   - Частота: Постоянно
   - Тип: AdMob Banner

2. **Interstitial реклама (Полноэкранная)**
   - Размещение: После успешной обработки изображения
   - Частота: Каждые 2-3 обработки
   - Тип: AdMob Interstitial

3. **Rewarded реклама (С наградой)**
   - Размещение: Опционально перед обработкой (для бесплатных пользователей)
   - Награда: Пропуск рекламы на 1 час
   - Тип: AdMob Rewarded / ironSource Rewarded

4. **Native реклама (Нативная)**
   - Размещение: В списке настроек (опционально)
   - Тип: AdMob Native

### Стратегия показа:

- **Banner**: Всегда видим внизу экрана
- **Interstitial**: После сохранения обработанного изображения (каждые 2-3 раза)
- **Rewarded**: Опционально, если пользователь хочет пропустить рекламу

### Рекламные сети:

1. **AdMob** (основная сеть)
   - Banner, Interstitial, Rewarded, Native

2. **ironSource** (медиация)
   - Rewarded, Interstitial
   - Используется как fallback для AdMob

---

## 🔧 Технические детали

### Аналитика:
- **AppMetrica**: Основная аналитика (российский рынок)
- **AppsFlyer**: Attribution и маркетинговая аналитика
- **Firebase Analytics**: Дополнительная аналитика (Google)

### Реклама:
- **AdMob**: Основная рекламная сеть
- **ironSource**: Медиация и дополнительные рекламные сети

### Конфигурация:
- Все ключи и ID будут храниться в конфигурационных файлах
- Для разработки используются test ad unit IDs
- Для продакшена - реальные ad unit IDs из AdMob/ironSource

---

## 📝 Примечания

1. **Приватность**: API ключи пользователей НЕ отправляются в аналитику
2. **GDPR/CCPA**: Необходимо добавить согласие на обработку данных
3. **Тестирование**: Использовать test ad unit IDs во время разработки
4. **Мониторинг**: Отслеживать ошибки загрузки рекламы и оптимизировать размещение

