# 📱 Полная инструкция по использованию приложения

## 🚀 Что нужно для работы приложения

### 1. Установка Flutter (если еще не установлен)
- Скачайте Flutter с официального сайта: https://flutter.dev
- Установите и настройте согласно инструкции
- Проверьте установку: `flutter doctor`

### 2. Получение API ключа (ОБЯЗАТЕЛЬНО!)

Приложение использует внешние AI-сервисы для удаления фона. Вам нужен API ключ от одного из провайдеров:

#### Вариант 1: Remove.bg (РЕКОМЕНДУЕТСЯ - самый простой)
1. Перейдите на https://www.remove.bg/api
2. Нажмите "Get API Key" или "Sign Up"
3. Зарегистрируйтесь (можно через Google/GitHub)
4. После регистрации вы получите **бесплатный API ключ**
   - Бесплатный план: **50 запросов в месяц**
   - Ключ будет выглядеть примерно так: `abc123xyz456...`
5. Скопируйте ключ

#### Вариант 2: PhotoRoom
1. Перейдите на https://www.photoroom.com/api/
2. Зарегистрируйтесь
3. Получите API ключ
4. В приложении выберите "PhotoRoom" в списке провайдеров

#### Вариант 3: Clipdrop
1. Перейдите на https://clipdrop.co/api
2. Зарегистрируйтесь
3. Получите API ключ
4. В приложении выберите "Clipdrop" в списке провайдеров

---

## 🔧 Как запустить приложение

### Шаг 1: Установка зависимостей
```bash
flutter pub get
```

### Шаг 2: Запуск приложения
```bash
# Для Android/iOS
flutter run

# Для веб-браузера
flutter run -d chrome

# Для конкретного устройства
flutter devices  # посмотреть доступные устройства
flutter run -d <device_id>
```

---

## 📖 Как работает приложение (пошагово)

### 🎯 Общий принцип работы:

```
Выбор изображения → Отправка на AI API → Получение результата → Сохранение
```

### 📝 Детальный процесс:

#### **ШАГ 1: Запуск приложения**
1. Приложение открывается с главным экраном
2. Вы видите:
   - Поле для ввода API ключа
   - Выпадающий список выбора провайдера (Remove.bg, PhotoRoom, Clipdrop)
   - Кнопки "Gallery" и "Camera"

#### **ШАГ 2: Настройка API**
1. **Введите API ключ** в текстовое поле (он будет скрыт точками для безопасности)
2. **Выберите провайдера** из списка (по умолчанию - Remove.bg)
3. Ключ сохраняется только в памяти приложения (не на диск)

#### **ШАГ 3: Выбор изображения**
1. Нажмите **"Gallery"** - откроется галерея фотографий
   - Или нажмите **"Camera"** - откроется камера для съемки
2. Выберите изображение
3. Изображение **загружается в память** как массив байтов (Uint8List)
4. Изображение **отображается** на экране под кнопками

#### **ШАГ 4: Обработка изображения**

**Вариант A: Удаление фона**
1. Нажмите кнопку **"Remove Background"**
2. Приложение:
   - Проверяет наличие изображения и API ключа
   - Показывает индикатор загрузки
   - Отправляет байты изображения на выбранный API сервис
   - API обрабатывает изображение с помощью AI
   - Получает результат (изображение с прозрачным фоном)
   - Отображает результат на экране

**Вариант B: Размытие фона**
1. Настройте **радиус размытия** слайдером (1-50)
2. Нажмите кнопку **"Blur Background"**
3. Приложение:
   - Сначала удаляет фон через API (как в варианте A)
   - Загружает оригинальное изображение
   - Размывает оригинальное изображение локально
   - Накладывает объект без фона на размытое изображение
   - Отображает результат

#### **ШАГ 5: Сохранение результата**
1. После обработки вы увидите обработанное изображение
2. Нажмите кнопку **"Save Image"**
3. Изображение сохраняется в:
   - **Android/iOS**: директория документов приложения
   - Путь будет показан в уведомлении

---

## 🔍 Технические детали работы

### Архитектура приложения:

```
lib/
├── main.dart                    # Главный экран и UI
└── services/
    └── background_service.dart  # Логика работы с API
```

### Как работает BackgroundService:

1. **removeBackgroundFromBytes()**:
   - Принимает байты изображения
   - Создает HTTP Multipart запрос
   - Отправляет на выбранный API (Remove.bg/PhotoRoom/Clipdrop)
   - Получает PNG с прозрачным фоном
   - Возвращает байты обработанного изображения

2. **blurBackgroundFromBytes()**:
   - Вызывает removeBackgroundFromBytes() для удаления фона
   - Декодирует оригинальное изображение
   - Применяет Gaussian blur к оригиналу
   - Композирует: размытый фон + объект без фона
   - Возвращает результат

### Поток данных:

```
Пользователь выбирает изображение
    ↓
XFile (из image_picker)
    ↓
readAsBytes() → Uint8List (байты)
    ↓
BackgroundService.removeBackgroundFromBytes()
    ↓
HTTP MultipartRequest с байтами
    ↓
AI API сервер (Remove.bg/PhotoRoom/Clipdrop)
    ↓
Обработка AI → PNG с прозрачным фоном
    ↓
HTTP Response → Uint8List (байты результата)
    ↓
Image.memory() → отображение на экране
    ↓
Сохранение в файл (опционально)
```

---

## ⚠️ Важные моменты

### Что нужно обязательно:
- ✅ **API ключ** - без него приложение не будет работать
- ✅ **Интернет соединение** - для отправки запросов к API
- ✅ **Разрешения** на камеру/галерею (запрашиваются автоматически)

### Ограничения:
- ⚠️ Бесплатный план Remove.bg: **50 запросов в месяц**
- ⚠️ Размытие фона требует **два запроса**: удаление фона + обработка
- ⚠️ Большие изображения могут обрабатываться дольше
- ⚠️ На веб-платформе сохранение файлов работает по-другому

### Советы:
- 💡 Начните с Remove.bg - самый простой и надежный
- 💡 Для тестирования используйте небольшие изображения
- 💡 Сохраняйте API ключ в безопасном месте
- 💡 Следите за лимитом запросов на бесплатном плане

---

## 🐛 Решение проблем

### Ошибка "API key is required"
- Проверьте, что вы ввели ключ в поле "API Key"
- Убедитесь, что ключ скопирован полностью

### Ошибка "Failed to process image"
- Проверьте интернет соединение
- Убедитесь, что API ключ действителен
- Проверьте лимит запросов на вашем аккаунте

### Ошибка при выборе изображения
- Проверьте разрешения приложения (камера/галерея)
- На Android: разрешения запрашиваются автоматически
- На iOS: разрешения настраиваются в Info.plist (уже настроено)

### Приложение не запускается
- Выполните `flutter clean`
- Затем `flutter pub get`
- Проверьте `flutter doctor`

---

## 📞 Поддержка API провайдеров

- **Remove.bg**: https://www.remove.bg/api
- **PhotoRoom**: https://www.photoroom.com/api/
- **Clipdrop**: https://clipdrop.co/api

---

## 🎉 Готово!

Теперь вы знаете, как работает приложение от начала до конца. Просто:
1. Получите API ключ
2. Запустите приложение
3. Введите ключ
4. Выберите изображение
5. Обработайте и сохраните!

Удачи! 🚀

