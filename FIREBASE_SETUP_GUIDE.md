# 🔥 Настройка Firebase для приложения

## 📋 У вас есть 2 варианта:

### Вариант 1: Использовать существующий проект

Если у вас уже есть подходящий проект, выберите его из списка.

### Вариант 2: Создать новый проект (рекомендуется)

Создайте новый проект специально для этого приложения.

## 🚀 Пошаговая инструкция:

### Шаг 1: Запустите команду настройки

```bash
flutterfire configure
```

### Шаг 2: Выберите проект

Вы увидите список проектов:
```
? Select a Firebase project ›
❯ angular-firebase-app-6b4bd (angular-firebase-app)
  crypto-muffintech-ptrsco (CryptoMuffintech)
  ffffffffffffffffw-df560 (to-do)
  flutter-kanban-c5c23 (flutter-kanban)
  lpo-148f9 (lpo-)
  note-ca813 (Note)
  pizza-shop-w4gc1x (CakeForYou)
  <create a new project>
```

**Выберите:**
- **Существующий проект** (если подходит)
- **`<create a new project>`** (если хотите создать новый)

### Шаг 3: Если создаете новый проект

1. Введите название проекта (например: `background-editor-app`)
2. Дождитесь создания проекта

### Шаг 4: Выберите платформы

Команда спросит, какие платформы настроить:
- ✅ **Android** (обязательно)
- ✅ **iOS** (если планируете iOS)
- ⚠️ **Web** (опционально)

### Шаг 5: Подтвердите настройки

Команда автоматически:
- ✅ Скачает `google-services.json` для Android
- ✅ Скачает `GoogleService-Info.plist` для iOS
- ✅ Поместит файлы в нужные папки
- ✅ Настроит конфигурацию

## ⚙️ После настройки:

### 1. Включите Email/Password аутентификацию:

1. Откройте: https://console.firebase.google.com/
2. Выберите ваш проект
3. Перейдите: **Authentication** → **Sign-in method**
4. Нажмите на **Email/Password**
5. Включите **"Enable"**
6. Нажмите **"Save"**

### 2. Проверьте файлы:

Убедитесь, что файлы созданы:
- ✅ `android/app/google-services.json` (должен существовать)
- ✅ `ios/Runner/GoogleService-Info.plist` (если настроили iOS)

## 🎯 Быстрая команда:

```bash
# Запустите и следуйте инструкциям
flutterfire configure

# Выберите "<create a new project>" или существующий
# Выберите платформы (Android, iOS)
# Готово!
```

## ✅ Проверка:

После настройки проверьте:

```bash
# Проверьте, что файлы созданы
ls android/app/google-services.json
ls ios/Runner/GoogleService-Info.plist  # если iOS

# Запустите приложение
flutter run
```

## 🆘 Если что-то пошло не так:

### Ошибка "project not found":
- Используйте `flutterfire configure` без указания проекта
- Выберите проект из списка или создайте новый

### Файлы не созданы:
- Запустите команду еще раз
- Убедитесь, что выбрали правильные платформы

### Аутентификация не работает:
- Проверьте, что Email/Password включен в Firebase Console
- Убедитесь, что файлы на месте

---

**Готово!** После выполнения `flutterfire configure` все настроится автоматически! 🎉


