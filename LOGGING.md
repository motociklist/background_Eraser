# 📋 Логирование приложения

## Обзор

В приложении реализована система логирования важных действий, особенно для API/AI-интеграций. Используется пакет `logger` для структурированного логирования.

## Что логируется

### 🔌 API запросы
- **Запросы к API**: провайдер, endpoint, размер изображения, заголовки (API ключи скрыты)
- **Успешные ответы**: статус код, размер ответа, время выполнения
- **Ошибки API**: статус код, текст ошибки, время выполнения

### 🖼️ Обработка изображений
- Выбор изображения (источник, размер, путь)
- Удаление фона (параметры, время выполнения)
- Размытие фона (радиус размытия, время выполнения)
- Сохранение файлов (путь, размер, успех/ошибка)

### 🔄 Состояние приложения
- Инициализация контроллеров и экранов
- Изменения состояния (выбор провайдера, изменение радиуса размытия)
- Ошибки приложения

## Уровни логирования

- **Debug** (`Level.debug`): детальная информация (только в debug режиме)
- **Info** (`Level.info`): информационные сообщения
- **Warning** (`Level.warning`): предупреждения
- **Error** (`Level.error`): ошибки с stack trace

## Примеры логов

### API запрос
```
📡 API Request
Provider: Remove.bg
Endpoint: https://api.remove.bg/v1.0/removebg
Headers: {X-Api-Key: ***}
Image size: 2.5 MB
```

### Успешный ответ
```
✅ API Success
Provider: Remove.bg
Status: 200
Response size: 1.2 MB
Duration: 1250ms
```

### Ошибка API
```
❌ API Error
Provider: Remove.bg
Status: 401
Error: Invalid API key
Duration: 500ms
```

### Обработка изображения
```
🖼️ Image Processing: Remove Background
Image size: 2.5 MB
Parameters: {provider: removebg, duration_ms: 1250}
```

## Безопасность

- **API ключи автоматически скрываются** в логах (показываются только первые 8 символов)
- Чувствительные данные (токены, ключи) не попадают в логи

## Настройка

Логирование настраивается в `lib/services/logger_service.dart`:

```dart
Logger(
  printer: PrettyPrinter(
    methodCount: 2,        // Количество методов в stack trace
    errorMethodCount: 8,   // Количество методов в ошибках
    lineLength: 120,       // Длина строки
    colors: true,          // Цветной вывод
    printEmojis: true,     // Эмодзи в логах
  ),
  level: kDebugMode ? Level.debug : Level.info,
);
```

## Где используются логи

1. **BackgroundService** (`lib/services/background_service.dart`)
   - Все API запросы (Remove.bg, PhotoRoom, Clipdrop)
   - Обработка изображений (удаление фона, размытие)

2. **ImageProcessingController** (`lib/controllers/image_processing_controller.dart`)
   - Выбор изображений
   - Обработка изображений
   - Изменения состояния

3. **BackgroundEditorPage** (`lib/screens/background_editor_page.dart`)
   - Сохранение файлов
   - Ошибки сохранения

4. **main.dart**
   - Инициализация приложения

## Просмотр логов

### В режиме разработки
Логи отображаются в консоли при запуске приложения:
```bash
flutter run
```

### В production
Логи уровня `info` и выше записываются в консоль. Для сохранения логов в файл можно добавить файловый appender в `LoggerService`.

## Рекомендации

1. **Не логируйте чувствительные данные** - API ключи автоматически скрываются
2. **Используйте правильный уровень** - debug для отладки, info для важных событий
3. **Добавляйте контекст** - параметры, размеры, время выполнения помогают в отладке
4. **Логируйте ошибки с stack trace** - это помогает быстро найти проблему

---

**Готово!** Теперь все важные действия приложения логируются, что значительно упрощает отладку и мониторинг работы API/AI-интеграций.

