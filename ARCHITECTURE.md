# 🏗️ Архитектура проекта

## 📁 Структура папок

```
lib/
├── main.dart                          # Точка входа приложения
├── models/                            # Модели данных
│   └── app_state.dart                 # Модель состояния приложения
├── controllers/                       # Контроллеры (бизнес-логика)
│   └── image_processing_controller.dart
├── screens/                           # Экраны приложения
│   └── background_editor_page.dart   # Главный экран
├── widgets/                           # Переиспользуемые виджеты
│   ├── api_key_input.dart             # Поле ввода API ключа
│   ├── provider_selector.dart         # Выбор API провайдера
│   ├── image_picker_buttons.dart      # Кнопки выбора изображения
│   ├── image_display.dart             # Отображение изображения
│   ├── process_buttons.dart           # Кнопки обработки
│   ├── blur_slider.dart               # Слайдер размытия
│   ├── error_message.dart             # Сообщение об ошибке
│   └── loading_indicator.dart         # Индикатор загрузки
├── services/                          # Сервисы (API, внешние сервисы)
│   └── background_service.dart        # Сервис работы с AI API
└── utils/                             # Утилиты
    ├── web_download.dart              # Скачивание для веб
    └── web_download_stub.dart         # Заглушка для мобильных
```

## 🎯 Принципы архитектуры

### 1. **Разделение ответственности**
- **Models** - только данные, без логики
- **Controllers** - бизнес-логика и управление состоянием
- **Screens** - композиция виджетов и навигация
- **Widgets** - переиспользуемые UI компоненты
- **Services** - работа с внешними API и сервисами

### 2. **Модель состояния (AppState)**
Централизованное управление состоянием через модель `AppState`:
```dart
class AppState {
  final Uint8List? selectedImageBytes;
  final Uint8List? processedImage;
  final bool isProcessing;
  final String? errorMessage;
  final String apiKey;
  final String selectedProvider;
  final double blurRadius;
}
```

### 3. **Контроллер (ImageProcessingController)**
- Управляет состоянием через `ChangeNotifier`
- Инкапсулирует всю бизнес-логику
- Обрабатывает ошибки и форматирует сообщения
- Не зависит от UI

### 4. **Виджеты-компоненты**
Каждый виджет - отдельный файл с четкой ответственностью:
- **StatelessWidget** - для простых виджетов
- Принимают только необходимые параметры
- Легко тестируются и переиспользуются

## 📊 Поток данных

```
User Action
    ↓
Screen (UI)
    ↓
Controller (Business Logic)
    ↓
Service (API Calls)
    ↓
State Update
    ↓
UI Rebuild
```

## 🔄 Пример использования

### В экране:
```dart
class BackgroundEditorPage extends StatefulWidget {
  @override
  State<BackgroundEditorPage> createState() => _BackgroundEditorPageState();
}

class _BackgroundEditorPageState extends State<BackgroundEditorPage> {
  late final ImageProcessingController _controller;

  @override
  void initState() {
    super.initState();
    _controller = ImageProcessingController();
    _controller.addListener(() => setState(() {}));
  }

  @override
  Widget build(BuildContext context) {
    return ApiKeyInput(controller: _controller.apiKeyController);
  }
}
```

### В контроллере:
```dart
class ImageProcessingController extends ChangeNotifier {
  AppState _state = const AppState();

  Future<void> removeBackground() async {
    _state = _state.copyWith(isProcessing: true);
    notifyListeners();

    // Логика обработки...

    _state = _state.copyWith(processedImage: result);
    notifyListeners();
  }
}
```

## ✅ Преимущества новой архитектуры

1. **Модульность** - каждый компонент в отдельном файле
2. **Переиспользуемость** - виджеты можно использовать в разных местах
3. **Тестируемость** - легко писать unit-тесты для контроллеров
4. **Читаемость** - код структурирован и понятен
5. **Масштабируемость** - легко добавлять новые функции
6. **Поддерживаемость** - изменения изолированы в отдельных модулях

## 🎨 Виджеты

### ApiKeyInput
Поле ввода API ключа с иконкой и валидацией

### ProviderSelector
Выпадающий список выбора API провайдера с иконками

### ImagePickerButtons
Две кнопки: Gallery и Camera

### ImageDisplay
Отображение изображения с рамкой и заголовком

### ProcessButtons
Кнопки "Remove Background" и "Blur Background"

### BlurSlider
Слайдер с отображением текущего значения

### ErrorMessage
Красивое отображение ошибок с иконкой

### LoadingIndicator
Индикатор загрузки с опциональным сообщением

## 🔧 Расширение проекта

### Добавление нового виджета:
1. Создайте файл в `lib/widgets/`
2. Наследуйтесь от `StatelessWidget` или `StatefulWidget`
3. Экспортируйте через `export` в `widgets.dart` (опционально)

### Добавление нового экрана:
1. Создайте файл в `lib/screens/`
2. Используйте контроллеры для логики
3. Композируйте из виджетов

### Добавление нового сервиса:
1. Создайте файл в `lib/services/`
2. Инкапсулируйте работу с API
3. Возвращайте типизированные данные

---

**Примечание**: Эта архитектура следует принципам Clean Architecture и SOLID, что делает код более поддерживаемым и масштабируемым.

